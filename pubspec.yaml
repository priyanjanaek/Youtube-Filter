name: travel_video_link_updater
description: A Flutter app to update YouTube video links.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^5.4.0
  http: ^0.14.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
